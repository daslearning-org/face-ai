# TODO List
Some planned to-do list with priority.

- On `android` only Photo permission is present. We can only upload images as of now. Need to add full File manager permissions if we need to upload other file types. [high]
- Add an API support into the app to provide face recognition services via API. [medium]
- Change the MDBoxLayout to vertical or horizontal depending on the windows size. [low]
